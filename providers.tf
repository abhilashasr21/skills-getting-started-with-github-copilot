# Keep provider configuration out of modules; configure providers in root examples or CI.
# Example provider configuration for Azure (uncomment and set values or rely on env/managed identity)

# provider "azurerm" {
#   features = {}
# }